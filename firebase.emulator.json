{
  "//": "QA-only Firebase config for local staff smoke-testing. Loads the Slice-8e-preview rules (members can LIST) and runs the Firestore + Auth emulators. Use with: firebase emulators:start --config firebase.emulator.json. See docs/staff/slice8-emulator-smoke-runbook.md.",
  "firestore": {
    "rules": "firestore.emulator.rules",
    "indexes": "firestore.indexes.json"
  },
  "emulators": {
    "firestore": {
      "port": 8080
    },
    "auth": {
      "port": 9099
    },
    "ui": {
      "enabled": true
    },
    "singleProjectMode": true
  }
}
