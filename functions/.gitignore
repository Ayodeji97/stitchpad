node_modules/
lib/
*.log
.firebase/
.env
.env.*
# Committed template of the non-secret env vars (public callback URLs). Real
# values and secrets stay in the ignored .env / Secret Manager.
!.env.example
.runtimeconfig.json
*.key.json
