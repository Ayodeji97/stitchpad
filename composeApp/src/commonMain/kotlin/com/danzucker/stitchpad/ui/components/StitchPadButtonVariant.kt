package com.danzucker.stitchpad.ui.components

/** Visual style of a [StitchPadButton]. */
enum class StitchPadButtonVariant {
    /** Filled primary CTA — `colorScheme.primary` / `onPrimary`. */
    Primary,

    /** Outlined secondary action — primary content on a bordered, transparent fill. */
    Secondary,
}
